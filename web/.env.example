# Copy this file to .env and fill in real values — never commit .env
# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง — ห้าม commit .env ขึ้น git

# This catalog reads bundled product data and does not need secrets.
